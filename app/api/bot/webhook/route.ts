import {NextResponse} from 'next/server'
export async function POST(req:Request){try{const update=await req.json(),message=update.message;if(!message?.chat?.id)return NextResponse.json({ok:true});if(message.text==='/start'){const token=process.env.TELEGRAM_BOT_TOKEN,url=process.env.NEXT_PUBLIC_APP_URL;if(!token||!url)throw new Error('Bot is not configured');const api='https:'+'//api.telegram.org/bot'+token+'/sendMessage';await fetch(api,{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({chat_id:message.chat.id,text:'Добро пожаловать в Noxiss Store. Откройте магазин кнопкой ниже.',reply_markup:{inline_keyboard:[[{text:'Открыть магазин',web_app:{url}}]]}})})}return NextResponse.json({ok:true})}catch{return NextResponse.json({ok:false},{status:400})}}
